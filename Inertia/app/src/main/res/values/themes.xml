<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Inertia" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Inertia" parent="Base.Theme.Inertia" />
    <!-- Base application theme. -->
    <style name="Base.Theme.DemoCalander" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DemoCalander" parent="Base.Theme.DemoCalander" />

    <style name="TimePickerDialogStyle" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="colorAccent">@color/green</item> <!-- Set your desired header color -->
    </style>

    <style name="DatePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/green</item>
        <item name="android:textColorPrimaryInverse">@color/white</item>
        <item name="android:buttonTint">@color/green</item>
    </style>
</resources>